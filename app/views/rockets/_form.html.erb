<%= form_with(model: rocket) do |form| %>
  <% if rocket.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rocket.errors.count, "error") %> prohibited this rocket from being saved:</h2>

      <ul>
        <% rocket.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <form>
    
    <div class="d-flex flex-column justify-content-center">
      <div class="mb-3">
        <%= form.label :name, style: "display: block", class: "form-label" %>
        <%= form.text_field :name, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :fuel, style: "display: block", class: "form-label"%>
        <%= form.text_field :fuel, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :country, style: "display: block", class: "form-label" %>
        <%= form.text_field :country, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :activity, style: "display: block", class: "form-label" %>
        <%= form.text_field :activity, class: "form-control" %>
      </div>

      <div class="mb-3">
        <%= form.label :img_url, style: "display: block", class: "form-label" %>
        <%= form.text_field :img_url, class: "form-control" %>
        <div  class="form-text">Añadir el url de la imagen</div>
      </div>


      <div class="mb-3">
        <%= form.label :category, style: "display: block", class: "form-label" %>
        <%= form.select :category_id, options_for_select(@categories.collect {|cat| [cat.name, cat.id]}) %>
      </div>

      <div class="d-flex justify-content-center ">
        <%= form.button "Crear nave", type: "submit", class: "btn btn-info text-white" %>
      </div>
    <% end %>
    </div>
  </form>


